name: Sync Design Tokens

on:
  repository_dispatch:
    types: [tokens-release]
  workflow_dispatch:

permissions:
  contents: write # Required to commit token files

jobs:
  sync-tokens:
    runs-on: ubuntu-latest
    env:
      STUDIO_SERVICE_TOKEN: ${{ secrets.STUDIO_SERVICE_TOKEN }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "24"

      - name: Enable Corepack
        run: corepack enable

      - name: Create config file
        run: |
          cat > .studio.json << 'EOF'
          {
            "$schema": "https://tokens.studio/schema/cli",
            "dependencies": {
              "core": {
                "project": "${{ secrets.TOKENS_PROJECT }}",
                "ref": { "type": "branch", "name": "main" },
                "output": "tokens"
              }
            }
          }
          EOF

      - name: Pull tokens from Studio
        run: pnpm dlx @tokens-studio/studio-cli tokens pull --ci

      - name: Install Terrazzo dependencies
        run: pnpm install --frozen-lockfile

      - name: Build CSS tokens with Terrazzo
        run: pnpm run build

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add tokens/ .studio.json

          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: sync design tokens from Studio"
            git push
          fi
